_target_: src.logger.CometMLWriter
project_name: "nvocoder"
workspace: null
run_name: "testing"
mode: "online"
loss_names: ["disc_loss", "gen_loss", "melspec_loss"] # which losses to log (useful for multi-loss tasks, s.a. GANs)
log_checkpoints: False # set to True if you want to log save_dir with checkpoints to W&B
id_length: 32 # length of the run_id
